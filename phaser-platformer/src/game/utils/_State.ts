export default class _State {
  constructor() {}

  enter(scene: Phaser.Scene, controller: any) {
    // enter state
  }

  update(time: any, deltaTime: any) {
    // update state
  }

  exit() {
    // exit state
  }
}
